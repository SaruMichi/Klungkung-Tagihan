/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package com.dimata.dslik.entity.contentdata;

/**
 *
 * @author m20n9
 */
import java.sql.*;
import com.dimata.util.lang.I_Language;
import com.dimata.qdep.db.*;
import com.dimata.qdep.entity.*;
import com.dimata.util.Command;
import java.util.Vector;

public class PstContentDataJenisPemeringkat extends DBHandler implements I_DBInterface, I_DBType, I_PersintentExc, I_Language {

    public static final String TBL_CONTENT_DATA_JENIS_PEMERINGKAT = "dslik_cd_jenis_pemeringkat";
    public static final int FLD_JENIS_PEMERINGKAT_OID = 0;
    public static final int FLD_NAMA_JENIS_PEMERINGKAT = 1;
    public static final int FLD_KODE_CORE_BANKING = 2;
    public static final int FLD_KODE_OJK = 3;
    public static String[] fieldNames = {
        "JENIS_PEMERINGKAT_OID",
        "NAMA_JENIS_PEMERINGKAT",
        "KODE_CORE_BANKING",
        "KODE_OJK"
    };
    public static int[] fieldTypes = {
        TYPE_LONG + TYPE_PK + TYPE_ID,
        TYPE_STRING,
        TYPE_STRING,
        TYPE_STRING
    };

    public PstContentDataJenisPemeringkat() {
    }

    public PstContentDataJenisPemeringkat(int i) throws DBException {
        super(new PstContentDataJenisPemeringkat());
    }

    public PstContentDataJenisPemeringkat(String sOid) throws DBException {
        super(new PstContentDataJenisPemeringkat(0));
        if (!locate(sOid)) {
            throw new DBException(this, DBException.RECORD_NOT_FOUND);
        } else {
            return;
        }
    }

    public PstContentDataJenisPemeringkat(long lOid) throws DBException {
        super(new PstContentDataJenisPemeringkat(0));
        String sOid = "0";
        try {
            sOid = String.valueOf(lOid);
        } catch (Exception e) {
            throw new DBException(this, DBException.RECORD_NOT_FOUND);
        }
        if (!locate(sOid)) {
            throw new DBException(this, DBException.RECORD_NOT_FOUND);
        } else {
            return;
        }
    }

    public int getFieldSize() {
        return fieldNames.length;
    }

    public String getTableName() {
        return TBL_CONTENT_DATA_JENIS_PEMERINGKAT;
    }

    public String[] getFieldNames() {
        return fieldNames;
    }

    public int[] getFieldTypes() {
        return fieldTypes;
    }

    public String getPersistentName() {
        return new PstContentDataJenisPemeringkat().getClass().getName();
    }

    public static ContentDataJenisPemeringkat fetchExc(long oid) throws DBException {
        try {
            ContentDataJenisPemeringkat entContentDataJenisPemeringkat = new ContentDataJenisPemeringkat();
            PstContentDataJenisPemeringkat pstContentDataJenisPemeringkat = new PstContentDataJenisPemeringkat(oid);
            entContentDataJenisPemeringkat.setOID(oid);
            entContentDataJenisPemeringkat.setNamaJenisPemeringkat(pstContentDataJenisPemeringkat.getString(FLD_NAMA_JENIS_PEMERINGKAT));
            entContentDataJenisPemeringkat.setKodeCoreBanking(pstContentDataJenisPemeringkat.getString(FLD_KODE_CORE_BANKING));
            entContentDataJenisPemeringkat.setKodeOjk(pstContentDataJenisPemeringkat.getString(FLD_KODE_OJK));
            return entContentDataJenisPemeringkat;
        } catch (DBException dbe) {
            throw dbe;
        } catch (Exception e) {
            throw new DBException(new PstContentDataJenisPemeringkat(0), DBException.UNKNOWN);
        }
    }

    public long fetchExc(Entity entity) throws Exception {
        ContentDataJenisPemeringkat entContentDataJenisPemeringkat = fetchExc(entity.getOID());
        entity = (Entity) entContentDataJenisPemeringkat;
        return entContentDataJenisPemeringkat.getOID();
    }

    public static synchronized long updateExc(ContentDataJenisPemeringkat entContentDataJenisPemeringkat) throws DBException {
        try {
            if (entContentDataJenisPemeringkat.getOID() != 0) {
                PstContentDataJenisPemeringkat pstContentDataJenisPemeringkat = new PstContentDataJenisPemeringkat(entContentDataJenisPemeringkat.getOID());
                pstContentDataJenisPemeringkat.setString(FLD_NAMA_JENIS_PEMERINGKAT, entContentDataJenisPemeringkat.getNamaJenisPemeringkat());
                pstContentDataJenisPemeringkat.setString(FLD_KODE_CORE_BANKING, entContentDataJenisPemeringkat.getKodeCoreBanking());
                pstContentDataJenisPemeringkat.setString(FLD_KODE_OJK, entContentDataJenisPemeringkat.getKodeOjk());
                pstContentDataJenisPemeringkat.update();
                return entContentDataJenisPemeringkat.getOID();
            }
        } catch (DBException dbe) {
            throw dbe;
        } catch (Exception e) {
            throw new DBException(new PstContentDataJenisPemeringkat(0), DBException.UNKNOWN);
        }
        return 0;
    }

    public long updateExc(Entity entity) throws Exception {
        return updateExc((ContentDataJenisPemeringkat) entity);
    }

    public static synchronized long deleteExc(long oid) throws DBException {
        try {
            PstContentDataJenisPemeringkat pstContentDataJenisPemeringkat = new PstContentDataJenisPemeringkat(oid);
            pstContentDataJenisPemeringkat.delete();
        } catch (DBException dbe) {
            throw dbe;
        } catch (Exception e) {
            throw new DBException(new PstContentDataJenisPemeringkat(0), DBException.UNKNOWN);
        }
        return oid;
    }

    public long deleteExc(Entity entity) throws Exception {
        if (entity == null) {
            throw new DBException(this, DBException.RECORD_NOT_FOUND);
        }
        return deleteExc(entity.getOID());
    }

    public static synchronized long insertExc(ContentDataJenisPemeringkat entContentDataJenisPemeringkat) throws DBException {
        try {
            PstContentDataJenisPemeringkat pstContentDataJenisPemeringkat = new PstContentDataJenisPemeringkat(0);
            pstContentDataJenisPemeringkat.setString(FLD_NAMA_JENIS_PEMERINGKAT, entContentDataJenisPemeringkat.getNamaJenisPemeringkat());
            pstContentDataJenisPemeringkat.setString(FLD_KODE_CORE_BANKING, entContentDataJenisPemeringkat.getKodeCoreBanking());
            pstContentDataJenisPemeringkat.setString(FLD_KODE_OJK, entContentDataJenisPemeringkat.getKodeOjk());
            pstContentDataJenisPemeringkat.insert();
            entContentDataJenisPemeringkat.setOID(pstContentDataJenisPemeringkat.getlong(FLD_JENIS_PEMERINGKAT_OID));
        } catch (DBException dbe) {
            throw dbe;
        } catch (Exception e) {
            throw new DBException(new PstContentDataJenisPemeringkat(0), DBException.UNKNOWN);
        }
        return entContentDataJenisPemeringkat.getOID();
    }

    public long insertExc(Entity entity) throws Exception {
        return insertExc((ContentDataJenisPemeringkat) entity);
    }

    public static void resultToObject(ResultSet rs, ContentDataJenisPemeringkat entContentDataJenisPemeringkat) {
        try {
            entContentDataJenisPemeringkat.setOID(rs.getLong(PstContentDataJenisPemeringkat.fieldNames[PstContentDataJenisPemeringkat.FLD_JENIS_PEMERINGKAT_OID]));
            entContentDataJenisPemeringkat.setNamaJenisPemeringkat(rs.getString(PstContentDataJenisPemeringkat.fieldNames[PstContentDataJenisPemeringkat.FLD_NAMA_JENIS_PEMERINGKAT]));
            entContentDataJenisPemeringkat.setKodeCoreBanking(rs.getString(PstContentDataJenisPemeringkat.fieldNames[PstContentDataJenisPemeringkat.FLD_KODE_CORE_BANKING]));
            entContentDataJenisPemeringkat.setKodeOjk(rs.getString(PstContentDataJenisPemeringkat.fieldNames[PstContentDataJenisPemeringkat.FLD_KODE_OJK]));
        } catch (Exception e) {
        }
    }

    public static Vector listAll() {
        return list(0, 500, "", "");
    }

    public static Vector list(int limitStart, int recordToGet, String whereClause, String order) {
        Vector lists = new Vector();
        DBResultSet dbrs = null;
        try {
            String sql = "SELECT * FROM " + TBL_CONTENT_DATA_JENIS_PEMERINGKAT;
            if (whereClause != null && whereClause.length() > 0) {
                sql = sql + " WHERE " + whereClause;
            }
            if (order != null && order.length() > 0) {
                sql = sql + " ORDER BY " + order;
            }
            if (limitStart == 0 && recordToGet == 0) {
                sql = sql + "";
            } else {
                sql = sql + " LIMIT " + limitStart + "," + recordToGet;
            }
            dbrs = DBHandler.execQueryResult(sql);
            ResultSet rs = dbrs.getResultSet();
            while (rs.next()) {
                ContentDataJenisPemeringkat entContentDataJenisPemeringkat = new ContentDataJenisPemeringkat();
                resultToObject(rs, entContentDataJenisPemeringkat);
                lists.add(entContentDataJenisPemeringkat);
            }
            rs.close();
            return lists;
        } catch (Exception e) {
            System.out.println(e);
        } finally {
            DBResultSet.close(dbrs);
        }
        return new Vector();
    }

    public static boolean checkOID(long entContentDataJenisPemeringkatId) {
        DBResultSet dbrs = null;
        boolean result = false;
        try {
            String sql = "SELECT * FROM " + TBL_CONTENT_DATA_JENIS_PEMERINGKAT + " WHERE "
                    + PstContentDataJenisPemeringkat.fieldNames[PstContentDataJenisPemeringkat.FLD_JENIS_PEMERINGKAT_OID] + " = " + entContentDataJenisPemeringkatId;
            dbrs = DBHandler.execQueryResult(sql);
            ResultSet rs = dbrs.getResultSet();
            while (rs.next()) {
                result = true;
            }
            rs.close();
        } catch (Exception e) {
            System.out.println("err : " + e.toString());
        } finally {
            DBResultSet.close(dbrs);
            return result;
        }
    }

    public static int getCount(String whereClause) {
        DBResultSet dbrs = null;
        try {
            String sql = "SELECT COUNT(" + PstContentDataJenisPemeringkat.fieldNames[PstContentDataJenisPemeringkat.FLD_JENIS_PEMERINGKAT_OID] + ") FROM " + TBL_CONTENT_DATA_JENIS_PEMERINGKAT;
            if (whereClause != null && whereClause.length() > 0) {
                sql = sql + " WHERE " + whereClause;
            }
            dbrs = DBHandler.execQueryResult(sql);
            ResultSet rs = dbrs.getResultSet();
            int count = 0;
            while (rs.next()) {
                count = rs.getInt(1);
            }
            rs.close();
            return count;
        } catch (Exception e) {
            return 0;
        } finally {
            DBResultSet.close(dbrs);
        }
    }

    public static int findLimitStart(long oid, int recordToGet, String whereClause, String orderClause) {
        int size = getCount(whereClause);
        int start = 0;
        boolean found = false;
        for (int i = 0; (i < size) && !found; i = i + recordToGet) {
            Vector list = list(i, recordToGet, whereClause, orderClause);
            start = i;
            if (list.size() > 0) {
                for (int ls = 0; ls < list.size(); ls++) {
                    ContentDataJenisPemeringkat entContentDataJenisPemeringkat = (ContentDataJenisPemeringkat) list.get(ls);
                    if (oid == entContentDataJenisPemeringkat.getOID()) {
                        found = true;
                    }
                }
            }
        }
        if ((start >= size) && (size > 0)) {
            start = start - recordToGet;
        }
        return start;
    }

    public static int findLimitCommand(int start, int recordToGet, int vectSize) {
        int cmd = Command.LIST;
        int mdl = vectSize % recordToGet;
        vectSize = vectSize + (recordToGet - mdl);
        if (start == 0) {
            cmd = Command.FIRST;
        } else {
            if (start == (vectSize - recordToGet)) {
                cmd = Command.LAST;
            } else {
                start = start + recordToGet;
                if (start <= (vectSize - recordToGet)) {
                    cmd = Command.NEXT;
                    System.out.println("next.......................");
                } else {
                    start = start - recordToGet;
                    if (start > 0) {
                        cmd = Command.PREV;
                        System.out.println("prev.......................");
                    }
                }
            }
        }
        return cmd;
    }
}
