/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package com.dimata.dslik.entity.contentdata;

/**
 *
 * @author m20n9
 */
import java.sql.*;
import com.dimata.util.lang.I_Language;
import com.dimata.qdep.db.*;
import com.dimata.qdep.entity.*;
import com.dimata.util.Command;
import java.util.Vector;

public class PstContentDataJenisPenggunaan extends DBHandler implements I_DBInterface, I_DBType, I_PersintentExc, I_Language {

    public static final String TBL_CONTENT_DATA_JENIS_PENGGUNAAN = "dslik_cd_jenis_penggunaan";
    public static final int FLD_JENIS_PENGGUNAAN_OID = 0;
    public static final int FLD_JENIS_PENGGUNAAN = 1;
    public static final int FLD_KODE_CORE_BANKING = 2;
    public static final int FLD_KODE_OJK = 3;
    public static String[] fieldNames = {
        "JENIS_PENGGUNAAN_OID",
        "JENIS_PENGGUNAAN",
        "KODE_CORE_BANKING",
        "KODE_OJK"
    };
    public static int[] fieldTypes = {
        TYPE_LONG + TYPE_PK + TYPE_ID,
        TYPE_STRING,
        TYPE_STRING,
        TYPE_STRING
    };

    public PstContentDataJenisPenggunaan() {
    }

    public PstContentDataJenisPenggunaan(int i) throws DBException {
        super(new PstContentDataJenisPenggunaan());
    }

    public PstContentDataJenisPenggunaan(String sOid) throws DBException {
        super(new PstContentDataJenisPenggunaan(0));
        if (!locate(sOid)) {
            throw new DBException(this, DBException.RECORD_NOT_FOUND);
        } else {
            return;
        }
    }

    public PstContentDataJenisPenggunaan(long lOid) throws DBException {
        super(new PstContentDataJenisPenggunaan(0));
        String sOid = "0";
        try {
            sOid = String.valueOf(lOid);
        } catch (Exception e) {
            throw new DBException(this, DBException.RECORD_NOT_FOUND);
        }
        if (!locate(sOid)) {
            throw new DBException(this, DBException.RECORD_NOT_FOUND);
        } else {
            return;
        }
    }

    public int getFieldSize() {
        return fieldNames.length;
    }

    public String getTableName() {
        return TBL_CONTENT_DATA_JENIS_PENGGUNAAN;
    }

    public String[] getFieldNames() {
        return fieldNames;
    }

    public int[] getFieldTypes() {
        return fieldTypes;
    }

    public String getPersistentName() {
        return new PstContentDataJenisPenggunaan().getClass().getName();
    }

    public static ContentDataJenisPenggunaan fetchExc(long oid) throws DBException {
        try {
            ContentDataJenisPenggunaan entContentDataJenisPenggunaan = new ContentDataJenisPenggunaan();
            PstContentDataJenisPenggunaan pstContentDataJenisPenggunaan = new PstContentDataJenisPenggunaan(oid);
            entContentDataJenisPenggunaan.setOID(oid);
            entContentDataJenisPenggunaan.setJenisPenggunaan(pstContentDataJenisPenggunaan.getString(FLD_JENIS_PENGGUNAAN));
            entContentDataJenisPenggunaan.setKodeCoreBanking(pstContentDataJenisPenggunaan.getString(FLD_KODE_CORE_BANKING));
            entContentDataJenisPenggunaan.setKodeOjk(pstContentDataJenisPenggunaan.getString(FLD_KODE_OJK));
            return entContentDataJenisPenggunaan;
        } catch (DBException dbe) {
            throw dbe;
        } catch (Exception e) {
            throw new DBException(new PstContentDataJenisPenggunaan(0), DBException.UNKNOWN);
        }
    }

    public long fetchExc(Entity entity) throws Exception {
        ContentDataJenisPenggunaan entContentDataJenisPenggunaan = fetchExc(entity.getOID());
        entity = (Entity) entContentDataJenisPenggunaan;
        return entContentDataJenisPenggunaan.getOID();
    }

    public static synchronized long updateExc(ContentDataJenisPenggunaan entContentDataJenisPenggunaan) throws DBException {
        try {
            if (entContentDataJenisPenggunaan.getOID() != 0) {
                PstContentDataJenisPenggunaan pstContentDataJenisPenggunaan = new PstContentDataJenisPenggunaan(entContentDataJenisPenggunaan.getOID());
                pstContentDataJenisPenggunaan.setString(FLD_JENIS_PENGGUNAAN, entContentDataJenisPenggunaan.getJenisPenggunaan());
                pstContentDataJenisPenggunaan.setString(FLD_KODE_CORE_BANKING, entContentDataJenisPenggunaan.getKodeCoreBanking());
                pstContentDataJenisPenggunaan.setString(FLD_KODE_OJK, entContentDataJenisPenggunaan.getKodeOjk());
                pstContentDataJenisPenggunaan.update();
                return entContentDataJenisPenggunaan.getOID();
            }
        } catch (DBException dbe) {
            throw dbe;
        } catch (Exception e) {
            throw new DBException(new PstContentDataJenisPenggunaan(0), DBException.UNKNOWN);
        }
        return 0;
    }

    public long updateExc(Entity entity) throws Exception {
        return updateExc((ContentDataJenisPenggunaan) entity);
    }

    public static synchronized long deleteExc(long oid) throws DBException {
        try {
            PstContentDataJenisPenggunaan pstContentDataJenisPenggunaan = new PstContentDataJenisPenggunaan(oid);
            pstContentDataJenisPenggunaan.delete();
        } catch (DBException dbe) {
            throw dbe;
        } catch (Exception e) {
            throw new DBException(new PstContentDataJenisPenggunaan(0), DBException.UNKNOWN);
        }
        return oid;
    }

    public long deleteExc(Entity entity) throws Exception {
        if (entity == null) {
            throw new DBException(this, DBException.RECORD_NOT_FOUND);
        }
        return deleteExc(entity.getOID());
    }

    public static synchronized long insertExc(ContentDataJenisPenggunaan entContentDataJenisPenggunaan) throws DBException {
        try {
            PstContentDataJenisPenggunaan pstContentDataJenisPenggunaan = new PstContentDataJenisPenggunaan(0);
            pstContentDataJenisPenggunaan.setString(FLD_JENIS_PENGGUNAAN, entContentDataJenisPenggunaan.getJenisPenggunaan());
            pstContentDataJenisPenggunaan.setString(FLD_KODE_CORE_BANKING, entContentDataJenisPenggunaan.getKodeCoreBanking());
            pstContentDataJenisPenggunaan.setString(FLD_KODE_OJK, entContentDataJenisPenggunaan.getKodeOjk());
            pstContentDataJenisPenggunaan.insert();
            entContentDataJenisPenggunaan.setOID(pstContentDataJenisPenggunaan.getlong(FLD_JENIS_PENGGUNAAN_OID));
        } catch (DBException dbe) {
            throw dbe;
        } catch (Exception e) {
            throw new DBException(new PstContentDataJenisPenggunaan(0), DBException.UNKNOWN);
        }
        return entContentDataJenisPenggunaan.getOID();
    }

    public long insertExc(Entity entity) throws Exception {
        return insertExc((ContentDataJenisPenggunaan) entity);
    }

    public static void resultToObject(ResultSet rs, ContentDataJenisPenggunaan entContentDataJenisPenggunaan) {
        try {
            entContentDataJenisPenggunaan.setOID(rs.getLong(PstContentDataJenisPenggunaan.fieldNames[PstContentDataJenisPenggunaan.FLD_JENIS_PENGGUNAAN_OID]));
            entContentDataJenisPenggunaan.setJenisPenggunaan(rs.getString(PstContentDataJenisPenggunaan.fieldNames[PstContentDataJenisPenggunaan.FLD_JENIS_PENGGUNAAN]));
            entContentDataJenisPenggunaan.setKodeCoreBanking(rs.getString(PstContentDataJenisPenggunaan.fieldNames[PstContentDataJenisPenggunaan.FLD_KODE_CORE_BANKING]));
            entContentDataJenisPenggunaan.setKodeOjk(rs.getString(PstContentDataJenisPenggunaan.fieldNames[PstContentDataJenisPenggunaan.FLD_KODE_OJK]));
        } catch (Exception e) {
        }
    }

    
    public static void resultToObjectWithoutOid(ResultSet rs, ContentDataJenisPenggunaan entContentDataJenisPenggunaan) {
        try {
            entContentDataJenisPenggunaan.setJenisPenggunaan(rs.getString(PstContentDataJenisPenggunaan.fieldNames[PstContentDataJenisPenggunaan.FLD_JENIS_PENGGUNAAN]));
            entContentDataJenisPenggunaan.setKodeOjk(rs.getString(PstContentDataJenisPenggunaan.fieldNames[PstContentDataJenisPenggunaan.FLD_KODE_OJK]));
        } catch (Exception e) {
        }
    }

    
    
    public static void resultToObjectTanpaOid(ResultSet rs, ContentDataJenisPenggunaan entContentDataJenisPenggunaan) {
        try {
            entContentDataJenisPenggunaan.setJenisPenggunaan(rs.getString(PstContentDataJenisPenggunaan.fieldNames[PstContentDataJenisPenggunaan.FLD_JENIS_PENGGUNAAN]));
            
            entContentDataJenisPenggunaan.setKodeOjk(rs.getString(PstContentDataJenisPenggunaan.fieldNames[PstContentDataJenisPenggunaan.FLD_KODE_OJK]));
        } catch (Exception e) {
        }
    }


    public static Vector listAll() {
        return list(0, 500, "", "");
    }

    public static Vector list(int limitStart, int recordToGet, String whereClause, String order) {
        Vector lists = new Vector();
        DBResultSet dbrs = null;
        try {
            String sql = "SELECT * FROM " + TBL_CONTENT_DATA_JENIS_PENGGUNAAN;
            if (whereClause != null && whereClause.length() > 0) {
                sql = sql + " WHERE " + whereClause;
            }
            if (order != null && order.length() > 0) {
                sql = sql + " ORDER BY " + order;
            }
            if (limitStart == 0 && recordToGet == 0) {
                sql = sql + "";
            } else {
                sql = sql + " LIMIT " + limitStart + "," + recordToGet;
            }
            dbrs = DBHandler.execQueryResult(sql);
            ResultSet rs = dbrs.getResultSet();
            while (rs.next()) {
                ContentDataJenisPenggunaan entContentDataJenisPenggunaan = new ContentDataJenisPenggunaan();
                resultToObject(rs, entContentDataJenisPenggunaan);
                lists.add(entContentDataJenisPenggunaan);
            }
            rs.close();
            return lists;
        } catch (Exception e) {
            System.out.println(e);
        } finally {
            DBResultSet.close(dbrs);
        }
        return new Vector();
    }

    
    public static Vector listWithoutOid(int limitStart, int recordToGet, String whereClause, String order) {
        Vector lists = new Vector();
        DBResultSet dbrs = null;
        try {
            String sql = "SELECT DISTINCT "+fieldNames[FLD_JENIS_PENGGUNAAN]+","
                    + fieldNames[FLD_KODE_OJK]+" "
                    + "FROM " + TBL_CONTENT_DATA_JENIS_PENGGUNAAN;
            if (whereClause != null && whereClause.length() > 0) {
                sql = sql + " WHERE " + whereClause;
            }
            if (order != null && order.length() > 0) {
                sql = sql + " ORDER BY " + order;
            }
            if (limitStart == 0 && recordToGet == 0) {
                sql = sql + "";
            } else {
                sql = sql + " LIMIT " + limitStart + "," + recordToGet;
            }
            dbrs = DBHandler.execQueryResult(sql);
            ResultSet rs = dbrs.getResultSet();
            while (rs.next()) {
                ContentDataJenisPenggunaan entContentDataJenisPenggunaan = new ContentDataJenisPenggunaan();
                resultToObjectWithoutOid(rs, entContentDataJenisPenggunaan);
                lists.add(entContentDataJenisPenggunaan);
            }
            rs.close();
            return lists;
        } catch (Exception e) {
            System.out.println(e);
        } finally {
            DBResultSet.close(dbrs);
        }
        return new Vector();
    }
    

    
    
    public static Vector listTanpaOid(int limitStart, int recordToGet, String whereClause, String order) {
        Vector lists = new Vector();
        DBResultSet dbrs = null;
        try {
            String sql = "SELECT DISTINCT JENIS_PENGGUNAAN, KODE_OJK FROM " + TBL_CONTENT_DATA_JENIS_PENGGUNAAN;
            if (whereClause != null && whereClause.length() > 0) {
                sql = sql + " WHERE " + whereClause;
            }
            if (order != null && order.length() > 0) {
                sql = sql + " ORDER BY " + order;
            }
            if (limitStart == 0 && recordToGet == 0) {
                sql = sql + "";
            } else {
                sql = sql + " LIMIT " + limitStart + "," + recordToGet;
            }
            dbrs = DBHandler.execQueryResult(sql);
            ResultSet rs = dbrs.getResultSet();
            while (rs.next()) {
                ContentDataJenisPenggunaan entContentDataJenisPenggunaan = new ContentDataJenisPenggunaan();
                resultToObjectTanpaOid(rs, entContentDataJenisPenggunaan);
                lists.add(entContentDataJenisPenggunaan);
            }
            rs.close();
            return lists;
        } catch (Exception e) {
            System.out.println(e);
        } finally {
            DBResultSet.close(dbrs);
        }
        return new Vector();
    }


    public static boolean checkOID(long entContentDataJenisPenggunaanId) {
        DBResultSet dbrs = null;
        boolean result = false;
        try {
            String sql = "SELECT * FROM " + TBL_CONTENT_DATA_JENIS_PENGGUNAAN + " WHERE "
                    + PstContentDataJenisPenggunaan.fieldNames[PstContentDataJenisPenggunaan.FLD_JENIS_PENGGUNAAN_OID] + " = " + entContentDataJenisPenggunaanId;
            dbrs = DBHandler.execQueryResult(sql);
            ResultSet rs = dbrs.getResultSet();
            while (rs.next()) {
                result = true;
            }
            rs.close();
        } catch (Exception e) {
            System.out.println("err : " + e.toString());
        } finally {
            DBResultSet.close(dbrs);
            return result;
        }
    }

    public static int getCount(String whereClause) {
        DBResultSet dbrs = null;
        try {
            String sql = "SELECT COUNT(" + PstContentDataJenisPenggunaan.fieldNames[PstContentDataJenisPenggunaan.FLD_JENIS_PENGGUNAAN_OID] + ") FROM " + TBL_CONTENT_DATA_JENIS_PENGGUNAAN;
            if (whereClause != null && whereClause.length() > 0) {
                sql = sql + " WHERE " + whereClause;
            }
            dbrs = DBHandler.execQueryResult(sql);
            ResultSet rs = dbrs.getResultSet();
            int count = 0;
            while (rs.next()) {
                count = rs.getInt(1);
            }
            rs.close();
            return count;
        } catch (Exception e) {
            return 0;
        } finally {
            DBResultSet.close(dbrs);
        }
    }

    public static int findLimitStart(long oid, int recordToGet, String whereClause, String orderClause) {
        int size = getCount(whereClause);
        int start = 0;
        boolean found = false;
        for (int i = 0; (i < size) && !found; i = i + recordToGet) {
            Vector list = list(i, recordToGet, whereClause, orderClause);
            start = i;
            if (list.size() > 0) {
                for (int ls = 0; ls < list.size(); ls++) {
                    ContentDataJenisPenggunaan entContentDataJenisPenggunaan = (ContentDataJenisPenggunaan) list.get(ls);
                    if (oid == entContentDataJenisPenggunaan.getOID()) {
                        found = true;
                    }
                }
            }
        }
        if ((start >= size) && (size > 0)) {
            start = start - recordToGet;
        }
        return start;
    }

    public static int findLimitCommand(int start, int recordToGet, int vectSize) {
        int cmd = Command.LIST;
        int mdl = vectSize % recordToGet;
        vectSize = vectSize + (recordToGet - mdl);
        if (start == 0) {
            cmd = Command.FIRST;
        } else {
            if (start == (vectSize - recordToGet)) {
                cmd = Command.LAST;
            } else {
                start = start + recordToGet;
                if (start <= (vectSize - recordToGet)) {
                    cmd = Command.NEXT;
                    System.out.println("next.......................");
                } else {
                    start = start - recordToGet;
                    if (start > 0) {
                        cmd = Command.PREV;
                        System.out.println("prev.......................");
                    }
                }
            }
        }
        return cmd;
    }
}
