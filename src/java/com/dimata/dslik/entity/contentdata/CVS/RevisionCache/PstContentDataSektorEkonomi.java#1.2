/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package com.dimata.dslik.entity.contentdata;

import java.sql.*;
import com.dimata.util.lang.I_Language;
import com.dimata.qdep.db.*;
import com.dimata.qdep.entity.*;
import com.dimata.util.Command;
import java.util.Vector;

/**
 *
 * @author Dewa
 */
public class PstContentDataSektorEkonomi extends DBHandler implements I_DBInterface, I_DBType, I_PersintentExc, I_Language {

    public static final String TBL_CONTENT_DATA_SEKTOR_EKONOMI = "dslik_cd_sektor_ekonomi";
    public static final int FLD_SEKTOR_EKONOMI_OID = 0;
    public static final int FLD_NAMA_SEKTOR_EKONOMI = 1;
    public static final int FLD_KODE_CORE_BANKING = 2;
    public static final int FLD_KODE_OJK = 3;

    public static String[] fieldNames = {
        "SEKTOR_EKONOMI_OID",
        "NAMA_SEKTOR_EKONOMI",
        "KODE_CORE_BANKING",
        "KODE_OJK"
    };

    public static int[] fieldTypes = {
        TYPE_LONG + TYPE_PK + TYPE_ID,
        TYPE_STRING,
        TYPE_STRING,
        TYPE_STRING
    };

    public PstContentDataSektorEkonomi() {
    }

    public PstContentDataSektorEkonomi(int i) throws DBException {
        super(new PstContentDataSektorEkonomi());
    }

    public PstContentDataSektorEkonomi(String sOid) throws DBException {
        super(new PstContentDataSektorEkonomi(0));
        if (!locate(sOid)) {
            throw new DBException(this, DBException.RECORD_NOT_FOUND);
        } else {
            return;
        }
    }

    public PstContentDataSektorEkonomi(long lOid) throws DBException {
        super(new PstContentDataSektorEkonomi(0));
        String sOid = "0";
        try {
            sOid = String.valueOf(lOid);
        } catch (Exception e) {
            throw new DBException(this, DBException.RECORD_NOT_FOUND);
        }
        if (!locate(sOid)) {
            throw new DBException(this, DBException.RECORD_NOT_FOUND);
        } else {
            return;
        }
    }

    public int getFieldSize() {
        return fieldNames.length;
    }

    public String getTableName() {
        return TBL_CONTENT_DATA_SEKTOR_EKONOMI;
    }

    public String[] getFieldNames() {
        return fieldNames;
    }

    public int[] getFieldTypes() {
        return fieldTypes;
    }

    public String getPersistentName() {
        return new PstContentDataSektorEkonomi().getClass().getName();
    }

    public static ContentDataSektorEkonomi fetchExc(long oid) throws DBException {
        try {
            ContentDataSektorEkonomi entContentDataSektorEkonomi = new ContentDataSektorEkonomi();
            PstContentDataSektorEkonomi pstContentDataSektorEkonomi = new PstContentDataSektorEkonomi(oid);
            entContentDataSektorEkonomi.setOID(oid);
            entContentDataSektorEkonomi.setNamaSektorEkonomi(pstContentDataSektorEkonomi.getString(FLD_NAMA_SEKTOR_EKONOMI));
            entContentDataSektorEkonomi.setKodeCoreBanking(pstContentDataSektorEkonomi.getString(FLD_KODE_CORE_BANKING));
            entContentDataSektorEkonomi.setKodeOjk(pstContentDataSektorEkonomi.getString(FLD_KODE_OJK));
            return entContentDataSektorEkonomi;
        } catch (DBException dbe) {
            throw dbe;
        } catch (Exception e) {
            throw new DBException(new PstContentDataSektorEkonomi(0), DBException.UNKNOWN);
        }
    }

    public long fetchExc(Entity entity) throws Exception {
        ContentDataSektorEkonomi entContentDataSektorEkonomi = fetchExc(entity.getOID());
        entity = (Entity) entContentDataSektorEkonomi;
        return entContentDataSektorEkonomi.getOID();
    }

    public static synchronized long updateExc(ContentDataSektorEkonomi entContentDataSektorEkonomi) throws DBException {
        try {
            if (entContentDataSektorEkonomi.getOID() != 0) {
                PstContentDataSektorEkonomi pstContentDataSektorEkonomi = new PstContentDataSektorEkonomi(entContentDataSektorEkonomi.getOID());
                pstContentDataSektorEkonomi.setString(FLD_NAMA_SEKTOR_EKONOMI, entContentDataSektorEkonomi.getNamaSektorEkonomi());
                pstContentDataSektorEkonomi.setString(FLD_KODE_CORE_BANKING, entContentDataSektorEkonomi.getKodeCoreBanking());
                pstContentDataSektorEkonomi.setString(FLD_KODE_OJK, entContentDataSektorEkonomi.getKodeOjk());
                pstContentDataSektorEkonomi.update();
                return entContentDataSektorEkonomi.getOID();
            }
        } catch (DBException dbe) {
            throw dbe;
        } catch (Exception e) {
            throw new DBException(new PstContentDataSektorEkonomi(0), DBException.UNKNOWN);
        }
        return 0;
    }

    public long updateExc(Entity entity) throws Exception {
        return updateExc((ContentDataSektorEkonomi) entity);
    }

    public static synchronized long deleteExc(long oid) throws DBException {
        try {
            PstContentDataSektorEkonomi pstContentDataSektorEkonomi = new PstContentDataSektorEkonomi(oid);
            pstContentDataSektorEkonomi.delete();
        } catch (DBException dbe) {
            throw dbe;
        } catch (Exception e) {
            throw new DBException(new PstContentDataSektorEkonomi(0), DBException.UNKNOWN);
        }
        return oid;
    }

    public long deleteExc(Entity entity) throws Exception {
        if (entity == null) {
            throw new DBException(this, DBException.RECORD_NOT_FOUND);
        }
        return deleteExc(entity.getOID());
    }

    public static synchronized long insertExc(ContentDataSektorEkonomi entContentDataSektorEkonomi) throws DBException {
        try {
            PstContentDataSektorEkonomi pstContentDataSektorEkonomi = new PstContentDataSektorEkonomi(0);
            pstContentDataSektorEkonomi.setString(FLD_NAMA_SEKTOR_EKONOMI, entContentDataSektorEkonomi.getNamaSektorEkonomi());
            pstContentDataSektorEkonomi.setString(FLD_KODE_CORE_BANKING, entContentDataSektorEkonomi.getKodeCoreBanking());
            pstContentDataSektorEkonomi.setString(FLD_KODE_OJK, entContentDataSektorEkonomi.getKodeOjk());
            pstContentDataSektorEkonomi.insert();
            entContentDataSektorEkonomi.setOID(pstContentDataSektorEkonomi.getlong(FLD_SEKTOR_EKONOMI_OID));
        } catch (DBException dbe) {
            throw dbe;
        } catch (Exception e) {
            throw new DBException(new PstContentDataSektorEkonomi(0), DBException.UNKNOWN);
        }
        return entContentDataSektorEkonomi.getOID();
    }

    public long insertExc(Entity entity) throws Exception {
        return insertExc((ContentDataSektorEkonomi) entity);
    }

    public static void resultToObject(ResultSet rs, ContentDataSektorEkonomi entContentDataSektorEkonomi) {
        try {
            entContentDataSektorEkonomi.setOID(rs.getLong(PstContentDataSektorEkonomi.fieldNames[PstContentDataSektorEkonomi.FLD_SEKTOR_EKONOMI_OID]));
            entContentDataSektorEkonomi.setNamaSektorEkonomi(rs.getString(PstContentDataSektorEkonomi.fieldNames[PstContentDataSektorEkonomi.FLD_NAMA_SEKTOR_EKONOMI]));
            entContentDataSektorEkonomi.setKodeCoreBanking(rs.getString(PstContentDataSektorEkonomi.fieldNames[PstContentDataSektorEkonomi.FLD_KODE_CORE_BANKING]));
            entContentDataSektorEkonomi.setKodeOjk(rs.getString(PstContentDataSektorEkonomi.fieldNames[PstContentDataSektorEkonomi.FLD_KODE_OJK]));
        } catch (Exception e) {
        }
    }
    
    public static void resultToObjectWithoutOid(ResultSet rs, ContentDataSektorEkonomi entContentDataSektorEkonomi) {
        try {
            entContentDataSektorEkonomi.setNamaSektorEkonomi(rs.getString(PstContentDataSektorEkonomi.fieldNames[PstContentDataSektorEkonomi.FLD_NAMA_SEKTOR_EKONOMI]));
            entContentDataSektorEkonomi.setKodeOjk(rs.getString(PstContentDataSektorEkonomi.fieldNames[PstContentDataSektorEkonomi.FLD_KODE_OJK]));
        } catch (Exception e) {
        }
    }

    public static Vector listAll() {
        return list(0, 500, "", "");
    }

    public static Vector list(int limitStart, int recordToGet, String whereClause, String order) {
        Vector lists = new Vector();
        DBResultSet dbrs = null;
        try {
            String sql = "SELECT * FROM " + TBL_CONTENT_DATA_SEKTOR_EKONOMI;
            if (whereClause != null && whereClause.length() > 0) {
                sql = sql + " WHERE " + whereClause;
            }
            if (order != null && order.length() > 0) {
                sql = sql + " ORDER BY " + order;
            }
            if (limitStart == 0 && recordToGet == 0) {
                sql = sql + "";
            } else {
                sql = sql + " LIMIT " + limitStart + "," + recordToGet;
            }
            dbrs = DBHandler.execQueryResult(sql);
            ResultSet rs = dbrs.getResultSet();
            while (rs.next()) {
                ContentDataSektorEkonomi entContentDataSektorEkonomi = new ContentDataSektorEkonomi();
                resultToObject(rs, entContentDataSektorEkonomi);
                lists.add(entContentDataSektorEkonomi);
            }
            rs.close();
            return lists;
        } catch (Exception e) {
            System.out.println(e);
        } finally {
            DBResultSet.close(dbrs);
        }
        return new Vector();
    }
    
    public static Vector listWithoutOid(int limitStart, int recordToGet, String whereClause, String order) {
        Vector lists = new Vector();
        DBResultSet dbrs = null;
        try {
            String sql = "SELECT DISTINCT "+fieldNames[FLD_KODE_OJK]+","
                    + fieldNames[FLD_NAMA_SEKTOR_EKONOMI]+" FROM " + TBL_CONTENT_DATA_SEKTOR_EKONOMI;
            if (whereClause != null && whereClause.length() > 0) {
                sql = sql + " WHERE " + whereClause;
            }
            if (order != null && order.length() > 0) {
                sql = sql + " ORDER BY " + order;
            }
            if (limitStart == 0 && recordToGet == 0) {
                sql = sql + "";
            } else {
                sql = sql + " LIMIT " + limitStart + "," + recordToGet;
            }
            dbrs = DBHandler.execQueryResult(sql);
            ResultSet rs = dbrs.getResultSet();
            while (rs.next()) {
                ContentDataSektorEkonomi entContentDataSektorEkonomi = new ContentDataSektorEkonomi();
                resultToObjectWithoutOid(rs, entContentDataSektorEkonomi);
                lists.add(entContentDataSektorEkonomi);
            }
            rs.close();
            return lists;
        } catch (Exception e) {
            System.out.println(e);
        } finally {
            DBResultSet.close(dbrs);
        }
        return new Vector();
    }

    public static boolean checkOID(long entContentDataSektorEkonomiId) {
        DBResultSet dbrs = null;
        boolean result = false;
        try {
            String sql = "SELECT * FROM " + TBL_CONTENT_DATA_SEKTOR_EKONOMI + " WHERE "
                    + PstContentDataSektorEkonomi.fieldNames[PstContentDataSektorEkonomi.FLD_SEKTOR_EKONOMI_OID] + " = " + entContentDataSektorEkonomiId;
            dbrs = DBHandler.execQueryResult(sql);
            ResultSet rs = dbrs.getResultSet();
            while (rs.next()) {
                result = true;
            }
            rs.close();
        } catch (Exception e) {
            System.out.println("err : " + e.toString());
        } finally {
            DBResultSet.close(dbrs);
            return result;
        }
    }

    public static int getCount(String whereClause) {
        DBResultSet dbrs = null;
        try {
            String sql = "SELECT COUNT(" + PstContentDataSektorEkonomi.fieldNames[PstContentDataSektorEkonomi.FLD_SEKTOR_EKONOMI_OID] + ") FROM " + TBL_CONTENT_DATA_SEKTOR_EKONOMI;
            if (whereClause != null && whereClause.length() > 0) {
                sql = sql + " WHERE " + whereClause;
            }
            dbrs = DBHandler.execQueryResult(sql);
            ResultSet rs = dbrs.getResultSet();
            int count = 0;
            while (rs.next()) {
                count = rs.getInt(1);
            }
            rs.close();
            return count;
        } catch (Exception e) {
            return 0;
        } finally {
            DBResultSet.close(dbrs);
        }
    }

    public static int findLimitStart(long oid, int recordToGet, String whereClause, String orderClause) {
        int size = getCount(whereClause);
        int start = 0;
        boolean found = false;
        for (int i = 0; (i < size) && !found; i = i + recordToGet) {
            Vector list = list(i, recordToGet, whereClause, orderClause);
            start = i;
            if (list.size() > 0) {
                for (int ls = 0; ls < list.size(); ls++) {
                    ContentDataSektorEkonomi entContentDataSektorEkonomi = (ContentDataSektorEkonomi) list.get(ls);
                    if (oid == entContentDataSektorEkonomi.getOID()) {
                        found = true;
                    }
                }
            }
        }
        if ((start >= size) && (size > 0)) {
            start = start - recordToGet;
        }
        return start;
    }

    public static int findLimitCommand(int start, int recordToGet, int vectSize) {
        int cmd = Command.LIST;
        int mdl = vectSize % recordToGet;
        vectSize = vectSize + (recordToGet - mdl);
        if (start == 0) {
            cmd = Command.FIRST;
        } else {
            if (start == (vectSize - recordToGet)) {
                cmd = Command.LAST;
            } else {
                start = start + recordToGet;
                if (start <= (vectSize - recordToGet)) {
                    cmd = Command.NEXT;
                    System.out.println("next.......................");
                } else {
                    start = start - recordToGet;
                    if (start > 0) {
                        cmd = Command.PREV;
                        System.out.println("prev.......................");
                    }
                }
            }
        }
        return cmd;
    }
}
