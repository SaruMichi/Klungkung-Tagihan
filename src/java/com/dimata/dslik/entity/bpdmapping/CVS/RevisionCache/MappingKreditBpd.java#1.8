/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package com.dimata.dslik.entity.bpdmapping;

import com.dimata.qdep.entity.Entity;
import java.util.Date;

/**
 *
 * @author Dewa
 */
public class MappingKreditBpd extends Entity {

    private String flagDetail = "";
    private String noRekening = "";
    private String cif = "";
    private String kodeSifat = "";
    private String noAkadAwal = "";
    private Date tglAkadAwal = null;
    private String noAkadAkhir = "";
    private Date tglAkadAkhir = null;
    private int baruPerpanjangan = 0;
    private Date tglAwal = null;
    private Date tglMulai = null;
    private Date tglTempo = null;
    private String kodeJenisPenggunaan = "";
    private String kodeOrientasiPenggunaan = "";
    private String kodeSektorEkonomi = "";
    private String kodeKab = "";
    private double nilai = 0;
    private String kodeValuta = "";
    private int prosentaseBunga = 0;
    private String jenisBunga = "";
    private double plafonAwal = 0;
    private double realisasi = 0;
    private double denda = 0;
    private double bakiDebet = 0;
    private String kodeKolektibilitas = "";
    private Date tglMacet = null;
    private double tunggakanPokok = 0;
    private String kodeKondisi = "";
    private Date tglKondisi = null;
    private String kodeKantorCabang = "";
    
    private String kodeKategoriDebitur="";
    private String sumberDana="";
    private double tunggakanBunga=0;
    private double jumlahHariTunggakan=0;
    private double frekuensiTunggakan=0;
    private double frekuensiRestrukturisasi=0;
    private Date tanggalRestrukturisasiAwal=new Date();
    private Date tanggalRestrukturisasiAkhir=new Date();
    private long periodeId=0;
    private String applid="";
    private String kodeKreditProgramPemerintah="";
    private String kodeSkimAkadPembiayaan="";
    private  double plafon=0;
    private String kodeJenisKredit="";
    private String kodeSebabMacet="";
    
    private int statusDataPelaporan=0;
    
    public String getFlagDetail() {
        return flagDetail;
    }

    public void setFlagDetail(String flagDetail) {
        this.flagDetail = flagDetail;
    }

    public String getNoRekening() {
        return noRekening;
    }

    public void setNoRekening(String noRekening) {
        this.noRekening = noRekening;
    }

    public String getKodeSifat() {
        return kodeSifat;
    }

    public void setKodeSifat(String kodeSifat) {
        this.kodeSifat = kodeSifat;
    }

    public String getNoAkadAwal() {
        return noAkadAwal;
    }

    public void setNoAkadAwal(String noAkadAwal) {
        this.noAkadAwal = noAkadAwal;
    }

    public Date getTglAkadAwal() {
        return tglAkadAwal;
    }

    public void setTglAkadAwal(Date tglAkadAwal) {
        this.tglAkadAwal = tglAkadAwal;
    }

    public String getNoAkadAkhir() {
        return noAkadAkhir;
    }

    public void setNoAkadAkhir(String noAkadAkhir) {
        this.noAkadAkhir = noAkadAkhir;
    }

    public Date getTglAkadAkhir() {
        return tglAkadAkhir;
    }

    public void setTglAkadAkhir(Date tglAkadAkhir) {
        this.tglAkadAkhir = tglAkadAkhir;
    }

    public int getBaruPerpanjangan() {
        return baruPerpanjangan;
    }

    public void setBaruPerpanjangan(int baruPerpanjangan) {
        this.baruPerpanjangan = baruPerpanjangan;
    }

    public Date getTglAwal() {
        return tglAwal;
    }

    public void setTglAwal(Date tglAwal) {
        this.tglAwal = tglAwal;
    }

    public Date getTglMulai() {
        return tglMulai;
    }

    public void setTglMulai(Date tglMulai) {
        this.tglMulai = tglMulai;
    }

    public Date getTglTempo() {
        return tglTempo;
    }

    public void setTglTempo(Date tglTempo) {
        this.tglTempo = tglTempo;
    }

    public String getKodeJenisPenggunaan() {
        return kodeJenisPenggunaan;
    }

    public void setKodeJenisPenggunaan(String kodeJenisPenggunaan) {
        this.kodeJenisPenggunaan = kodeJenisPenggunaan;
    }

    public String getKodeOrientasiPenggunaan() {
        return kodeOrientasiPenggunaan;
    }

    public void setKodeOrientasiPenggunaan(String kodeOrientasiPenggunaan) {
        this.kodeOrientasiPenggunaan = kodeOrientasiPenggunaan;
    }


    public String getKodeKab() {
        return kodeKab;
    }

    public void setKodeKab(String kodeKab) {
        this.kodeKab = kodeKab;
    }

    public double getNilai() {
        return nilai;
    }

    public void setNilai(double nilai) {
        this.nilai = nilai;
    }

    public String getKodeValuta() {
        return kodeValuta;
    }

    public void setKodeValuta(String kodeValuta) {
        this.kodeValuta = kodeValuta;
    }

    public int getProsentaseBunga() {
        return prosentaseBunga;
    }

    public void setProsentaseBunga(int prosentaseBunga) {
        this.prosentaseBunga = prosentaseBunga;
    }

    public String getJenisBunga() {
        return jenisBunga;
    }

    public void setJenisBunga(String jenisBunga) {
        this.jenisBunga = jenisBunga;
    }

    public double getPlafonAwal() {
        return plafonAwal;
    }

    public void setPlafonAwal(double plafonAwal) {
        this.plafonAwal = plafonAwal;
    }


    public double getRealisasi() {
        return realisasi;
    }

    public void setRealisasi(double realisasi) {
        this.realisasi = realisasi;
    }

    public double getDenda() {
        return denda;
    }

    public void setDenda(double denda) {
        this.denda = denda;
    }

    public double getBakiDebet() {
        return bakiDebet;
    }

    public void setBakiDebet(double bakiDebet) {
        this.bakiDebet = bakiDebet;
    }

    public String getKodeKolektibilitas() {
        return kodeKolektibilitas;
    }

    public void setKodeKolektibilitas(String kodeKolektibilitas) {
        this.kodeKolektibilitas = kodeKolektibilitas;
    }

    public Date getTglMacet() {
        return tglMacet;
    }

    public void setTglMacet(Date tglMacet) {
        this.tglMacet = tglMacet;
    }

    public double getTunggakanPokok() {
        return tunggakanPokok;
    }

    public void setTunggakanPokok(double tunggakanPokok) {
        this.tunggakanPokok = tunggakanPokok;
    }

    public String getKodeKondisi() {
        return kodeKondisi;
    }

    public void setKodeKondisi(String kodeKondisi) {
        this.kodeKondisi = kodeKondisi;
    }

    public Date getTglKondisi() {
        return tglKondisi;
    }

    public void setTglKondisi(Date tglKondisi) {
        this.tglKondisi = tglKondisi;
    }


    public String getKodeKantorCabang() {
        return kodeKantorCabang;
    }

    public void setKodeKantorCabang(String kodeKantorCabang) {
        this.kodeKantorCabang = kodeKantorCabang;
    }

    /**
     * @return the cif
     */
    public String getCif() {
        return cif;
    }

    /**
     * @param cif the cif to set
     */
    public void setCif(String cif) {
        this.cif = cif;
    }

    /**
     * @return the kodeKategoriDebitur
     */
    public String getKodeKategoriDebitur() {
        return kodeKategoriDebitur;
    }

    /**
     * @param kodeKategoriDebitur the kodeKategoriDebitur to set
     */
    public void setKodeKategoriDebitur(String kodeKategoriDebitur) {
        this.kodeKategoriDebitur = kodeKategoriDebitur;
    }

    /**
     * @return the sumberDana
     */
    public String getSumberDana() {
        return sumberDana;
    }

    /**
     * @param sumberDana the sumberDana to set
     */
    public void setSumberDana(String sumberDana) {
        this.sumberDana = sumberDana;
    }

    /**
     * @return the tunggakanBunga
     */
    public double getTunggakanBunga() {
        return tunggakanBunga;
    }

    /**
     * @param tunggakanBunga the tunggakanBunga to set
     */
    public void setTunggakanBunga(double tunggakanBunga) {
        this.tunggakanBunga = tunggakanBunga;
    }

    /**
     * @return the jumlahHariTunggakan
     */
    public double getJumlahHariTunggakan() {
        return jumlahHariTunggakan;
    }

    /**
     * @param jumlahHariTunggakan the jumlahHariTunggakan to set
     */
    public void setJumlahHariTunggakan(double jumlahHariTunggakan) {
        this.jumlahHariTunggakan = jumlahHariTunggakan;
    }

    /**
     * @return the frekuensiTunggakan
     */
    public double getFrekuensiTunggakan() {
        return frekuensiTunggakan;
    }

    /**
     * @param frekuensiTunggakan the frekuensiTunggakan to set
     */
    public void setFrekuensiTunggakan(double frekuensiTunggakan) {
        this.frekuensiTunggakan = frekuensiTunggakan;
    }

    /**
     * @return the frekuensiRestrukturisasi
     */
    public double getFrekuensiRestrukturisasi() {
        return frekuensiRestrukturisasi;
    }

    /**
     * @param frekuensiRestrukturisasi the frekuensiRestrukturisasi to set
     */
    public void setFrekuensiRestrukturisasi(double frekuensiRestrukturisasi) {
        this.frekuensiRestrukturisasi = frekuensiRestrukturisasi;
    }
    
    /**
     * @return the tanggalRestrukturisasiAwal
     */
    public Date getTanggalRestrukturisasiAwal() {
        return tanggalRestrukturisasiAwal;
    }

    /**
     * @param tanggalRestrukturisasiAwal the tanggalRestrukturisasiAwal to set
     */
    public void setTanggalRestrukturisasiAwal(Date tanggalRestrukturisasiAwal) {
        this.tanggalRestrukturisasiAwal = tanggalRestrukturisasiAwal;
    }

    /**
     * @return the tanggalRestrukturisasiAkhir
     */
    public Date getTanggalRestrukturisasiAkhir() {
        return tanggalRestrukturisasiAkhir;
    }

    /**
     * @param tanggalRestrukturisasiAkhir the tanggalRestrukturisasiAkhir to set
     */
    public void setTanggalRestrukturisasiAkhir(Date tanggalRestrukturisasiAkhir) {
        this.tanggalRestrukturisasiAkhir = tanggalRestrukturisasiAkhir;
    }

    /**
     * @return the periodeId
     */
    public long getPeriodeId() {
        return periodeId;
    }

    /**
     * @param periodeId the periodeId to set
     */
    public void setPeriodeId(long periodeId) {
        this.periodeId = periodeId;
    }

    /**
     * @return the applid
     */
    public String getApplid() {
        return applid;
    }

    /**
     * @param applid the applid to set
     */
    public void setApplid(String applid) {
        this.applid = applid;
    }

    /**
     * @return the kodeKreditProgramPemerintah
     */
    public String getKodeKreditProgramPemerintah() {
        return kodeKreditProgramPemerintah;
    }

    /**
     * @param kodeKreditProgramPemerintah the kodeKreditProgramPemerintah to set
     */
    public void setKodeKreditProgramPemerintah(String kodeKreditProgramPemerintah) {
        this.kodeKreditProgramPemerintah = kodeKreditProgramPemerintah;
    }

    /**
     * @return the kodeSkimAkadPembiayaan
     */
    public String getKodeSkimAkadPembiayaan() {
        return kodeSkimAkadPembiayaan;
    }

    /**
     * @param kodeSkimAkadPembiayaan the kodeSkimAkadPembiayaan to set
     */
    public void setKodeSkimAkadPembiayaan(String kodeSkimAkadPembiayaan) {
        this.kodeSkimAkadPembiayaan = kodeSkimAkadPembiayaan;
    }

    /**
     * @return the plafon
     */
    public double getPlafon() {
        return plafon;
    }

    /**
     * @param plafon the plafon to set
     */
    public void setPlafon(double plafon) {
        this.plafon = plafon;
    }

    /**
     * @return the kodeJenisKredit
     */
    public String getKodeJenisKredit() {
        return kodeJenisKredit;
    }

    /**
     * @param kodeJenisKredit the kodeJenisKredit to set
     */
    public void setKodeJenisKredit(String kodeJenisKredit) {
        this.kodeJenisKredit = kodeJenisKredit;
    }

    /**
     * @return the kodeSektorEkonomi
     */
    public String getKodeSektorEkonomi() {
        return kodeSektorEkonomi;
    }

    /**
     * @param kodeSektorEkonomi the kodeSektorEkonomi to set
     */
    public void setKodeSektorEkonomi(String kodeSektorEkonomi) {
        this.kodeSektorEkonomi = kodeSektorEkonomi;
    }

    /**
     * @return the kodeSebabMacet
     */
    public String getKodeSebabMacet() {
        return kodeSebabMacet;
    }

    /**
     * @param kodeSebabMacet the kodeSebabMacet to set
     */
    public void setKodeSebabMacet(String kodeSebabMacet) {
        this.kodeSebabMacet = kodeSebabMacet;
    }

    /**
     * @return the statusDataPelaporan
     */
    public int getStatusDataPelaporan() {
        return statusDataPelaporan;
    }

    /**
     * @param statusDataPelaporan the statusDataPelaporan to set
     */
    public void setStatusDataPelaporan(int statusDataPelaporan) {
        this.statusDataPelaporan = statusDataPelaporan;
    }
    
    
    public String getLogDetail(Entity prevDoc, Entity currentDoc) {
        //throw new UnsupportedOperationException("Not supported yet."); //To change body of generated methods, choose Tools | Templates.
        String history="";
        MappingKreditBpd prevMat = (MappingKreditBpd)prevDoc;
        MappingKreditBpd currMat = (MappingKreditBpd)currentDoc;
        try {
            //0
            if(prevMat == null || !prevMat.getFlagDetail().equals(currMat.getFlagDetail())){
                if (prevMat== null){
                    history=history+" Flag Detail : "+currMat.getFlagDetail()+"/n";
                }else{
                    history=history+" Flag Detail diubah dari "+prevMat.getFlagDetail()+" menjadi "+currMat.getFlagDetail()+"/n";
                }
            }
            
            //1
            if (prevMat == null || !prevMat.getNoRekening().equals(currMat.getNoRekening())) {
                if (prevMat== null){
                    history=history+" No. Rekening : "+currMat.getNoRekening()+"/n";
                }else{
                    history = history + " No. Rekening diubah dari " + prevMat.getNoRekening() + " menjadi " + currMat.getNoRekening() + "/n";
                }
            }
            
            //2
            if (prevMat == null || !prevMat.getCif().equals(currMat.getCif())) {
                if (prevMat== null){
                    history=history+" No. Rekening : "+currMat.getNoRekening()+"/n";
                }else{
                    history = history + " Cif diubah dari " + prevMat.getCif() + " menjadi " + currMat.getCif() + "/n";
                }
            }
            
            //3
            if (prevMat == null || !prevMat.getKodeSifat().equals(currMat.getKodeSifat())) {
                if (prevMat== null){
                    history=history+" Kode Sifat Kredit : "+currMat.getKodeSifat()+"/n";
                }else{
                    history = history + " Kode Sifat Kredit duibah dari dari " + prevMat.getKodeSifat() + " menjadi " + currMat.getKodeSifat() + "/n";
                }
            }
            
            //4
            if (prevMat == null || !prevMat.getKodeJenisKredit().equals(currMat.getKodeJenisKredit())) {
                if (prevMat== null){
                    history=history+" Kode Jenis Kredit : "+currMat.getKodeJenisKredit()+"/n";
                }else{
                    history = history + " Kode Jenis Kredit diubah dari " + prevMat.getKodeJenisKredit() + " menjadi " + currMat.getKodeJenisKredit() + "/n";
                }
            }
            
            //5
//            if (prevMat == null || !prevMat.getno.equals(currMat.getKodeAkad())) {
//                if (prevMat== null){
//                    history=history+" Kode Skim/Akad Pembiayaan : "+currMat.getKodeAkad()+"/n";
//                }else{
//                    history = history + " Kode Skim/Akad Pembiayaan diubah dari " + prevMat.getKodeAkad() + " menjadi " + currMat.getKodeAkad() + "/n";
//                }
//            }
            
            //6
            if (prevMat == null || !prevMat.getNoAkadAwal().equals(currMat.getNoAkadAwal())) {
                if (prevMat== null){
                    history=history+" No. Akad Awal : "+currMat.getNoAkadAwal()+"/n";
                }else{
                    history = history + " No. Akad Awal diubah dari " + prevMat.getNoAkadAwal() + " menjadi " + currMat.getNoAkadAwal() + "/n";
                }
            }
             
            //7
            if(prevMat == null || !prevMat.getTglAkadAwal().equals(currMat.getTglAkadAwal())){
                if (prevMat== null){
                    history=history+" Tanggal Akad Awal : "+currMat.getTglAkadAwal()+"/n";
                }else{
                    history=history+" Tanggal Akad Awal diubah dari "+prevMat.getTglAkadAwal()+" menjadi "+currMat.getTglAkadAwal()+"/n";
                }
            }
            
            //8
            if (prevMat == null || !prevMat.getNoAkadAkhir().equals(currMat.getNoAkadAkhir())) {
                if (prevMat== null){
                    history=history+" No. Akad Akhir : "+currMat.getNoAkadAkhir()+"/n";
                }else{
                    history = history + " No. Akad Akhir diubah dari " + prevMat.getNoAkadAkhir() + " menjadi " + currMat.getNoAkadAkhir() + "/n";
                }
            }
                    
            //9
            if (prevMat == null || !prevMat.getTglAkadAkhir().equals(currMat.getTglAkadAkhir())) {
                if (prevMat== null){
                    history=history+" Tanggal Akad Akhir : "+currMat.getTglAkadAkhir()+"/n";
                }else{
                    history = history + " Tanggal Akad Akhir diubah dari " + prevMat.getTglAkadAkhir() + " menjadi " + currMat.getTglAkadAkhir() + "/n";
                }
            }
            
            //10
            if (prevMat == null || prevMat.getBaruPerpanjangan() != currMat.getBaruPerpanjangan()) {
                if (prevMat== null){
                    history=history+" Baru Perpanjangan : "+currMat.getBaruPerpanjangan()+"/n";
                }else{
                    history = history + " Baru Perpanjangan diubah dari " + prevMat.getBaruPerpanjangan() + " menjadi " + currMat.getBaruPerpanjangan() + "/n";
                }
            }
            
            //11
            if (prevMat == null || !prevMat.getTglAwal().equals(currMat.getTglAkadAwal())) {
                if (prevMat== null){
                    history=history+" Tanggal Awal Kredit : "+currMat.getTglAkadAwal()+"/n";
                }else{
                    history = history + " Tanggal Awal Kredit diubah dari " + prevMat.getTglAwal() + " menjadi " + currMat.getTglAwal() + "/n";
                }
            }
            
            //12
            if (prevMat == null || !prevMat.getTglMulai().equals(currMat.getTglMulai())) {
                if (prevMat== null){
                    history=history+" Tanggal Mulai : "+currMat.getTglMulai()+"/n";
                }else{
                    history = history + " Tanggal Mulai diubah dari " + prevMat.getTglMulai() + " menjadi " + currMat.getTglMulai() + "/n";
                }
            }
            
            //13
            if (prevMat == null || !prevMat.getTglTempo().equals(currMat.getTglTempo())) {
                if (prevMat== null){
                    history=history+" Tanggal Jatuh Tempo : "+currMat.getTglTempo()+"/n";
                }else{
                    history = history + " Tanggal Jatuh Tempo diubah dari " + prevMat.getTglTempo() + " menjadi " + currMat.getTglTempo() + "/n";
                }
            }
            
            //14
            if (prevMat == null || !prevMat.getKodeKategoriDebitur().equals(currMat.getKodeKategoriDebitur())) {
                if (prevMat== null){
                    history=history+" Kode Kategori Debitur : "+currMat.getKodeKategoriDebitur()+"/n";
                }else{
                    history = history + " Kode Kategori Debitur diubah dari " + prevMat.getKodeKategoriDebitur() + " menjadi " + currMat.getKodeKategoriDebitur() + "/n";
                }
            }
            
            //15
            if (prevMat == null || !prevMat.getKodeJenisPenggunaan().equals(currMat.getKodeJenisPenggunaan())) {
                if (prevMat== null){
                    history=history+" Kode Jenis Penggunaan : "+currMat.getKodeJenisPenggunaan()+"/n";
                }else{
                    history = history + " Kode Jenis Penggunaan diubah dari " + prevMat.getKodeJenisPenggunaan() + " menjadi " + currMat.getKodeJenisPenggunaan() + "/n";
                }
            }
        
            //16
            if (prevMat == null || !prevMat.getKodeOrientasiPenggunaan().equals(currMat.getKodeOrientasiPenggunaan())) {
                if (prevMat== null){
                    history=history+" Kode Orientasi Penggunaan : "+currMat.getKodeOrientasiPenggunaan()+"/n";
                }else{
                    history = history + " Kode Orientasi Penggunaan dari " + prevMat.getKodeOrientasiPenggunaan() + " menjadi " + currMat.getKodeOrientasiPenggunaan() + "/n";
                }
            }
            
            //17
            if (prevMat == null || !prevMat.getKodeSektorEkonomi().equals(currMat.getKodeSektorEkonomi())) {
                if (prevMat== null){
                    history=history+" Kode Sektor Ekonomi : "+currMat.getKodeSektorEkonomi()+"/n";
                }else{
                    history = history + " Kode Sektor Ekonomi diubah dari " + prevMat.getKodeSektorEkonomi() + " menjadi " + currMat.getKodeSektorEkonomi() + "/n";
                }
            }
            
            //18
            if(prevMat == null || !prevMat.getKodeKab().equals(currMat.getKodeKab())){
                if (prevMat == null){
                    history=history+" Kode Kab/Kota (DATI 2) Lokasi Proyek/Penggunaan Kredit : "+currMat.getKodeKab()+"/n";
                }else{
                    history=history+" Kode Kab/Kota (DATI 2) Lokasi Proyek/Penggunaan Kredit diubah dari "+prevMat.getKodeKab()+" menjadi "+currMat.getKodeKab()+"/n";
                }
            }
            
            //19
            if (prevMat == null || (prevMat.getNilai() != currMat.getNilai())) {
                if (prevMat == null){
                    history=history+" Nilai Proyek : "+currMat.getNilai()+"/n";
                }else{
                    history = history + " Nilai Proyek diubah dari " + prevMat.getNilai() + " menjadi " + currMat.getNilai() + "/n";
                }
            }
                  
            //20
            if (prevMat == null || !prevMat.getKodeValuta().equals(currMat.getKodeValuta())) {
                if (prevMat == null){
                    history=history+" Kode Valuta : "+currMat.getKodeValuta()+"/n";
                }else{
                    history = history + " Kode Valuta diubah dari " + prevMat.getKodeValuta() + " menjadi " + currMat.getKodeValuta() + "/n";
                }
            }
            
            //21
            if (prevMat== null || prevMat.getProsentaseBunga() != currMat.getProsentaseBunga()) {
                if (prevMat== null){
                    history=history+" Prosentase Bunga / Imbalan : "+currMat.getProsentaseBunga()+"/n";
                }else{
                    history = history + " Prosentase Bunga / Imbalan diubah dari " + prevMat.getProsentaseBunga() + " menjadi " + currMat.getProsentaseBunga() + "/n";
                }
            }
            
            //22
            if (prevMat == null || !prevMat.getJenisBunga().equals(currMat.getJenisBunga())) {
                if (prevMat == null){
                    history=history+" Jenis Suku Bunga/Imbalan : "+currMat.getJenisBunga()+"/n";
                }else{
                    history = history + " Jenis Suku Bunga/Imbalan diubah dari " + prevMat.getJenisBunga() + " menjadi " + currMat.getJenisBunga() + "/n";
                }
            }
            
            //23
            if (prevMat == null || !prevMat.getKodeKreditProgramPemerintah().equals(currMat.getKodeKreditProgramPemerintah())) {
                if (prevMat== null){
                    history=history+" Kredit Program Pemerintah : "+currMat.getKodeKreditProgramPemerintah()+"/n";
                }else{
                    history = history + " Kredit Program Pemerintah diubah dari " + prevMat.getKodeKreditProgramPemerintah() + " menjadi " + currMat.getKodeKreditProgramPemerintah() + "/n";
                }
            }
            
            //24
//            if (prevMat == null || !prevMat.gettak().equals(this.getTakeover())) {
//                if (prevMat == null){
//                    history=history+" Takeover Dari : "+this.getTakeover()+"/n";
//                }else{
//                    history = history + " Takeover Dari diubah dari " + prevMat.getTakeover() + " menjadi " + this.getTakeover() + "/n";
//                }
//            }
            
            //25
            if (prevMat == null || !prevMat.getSumberDana().equals(currMat.getSumberDana())) {
                if (prevMat== null){
                    history=history+" Sumber Dana : "+currMat.getSumberDana()+"/n";
                }else{
                    history = history + " Sumber Dana diubah dari " + prevMat.getSumberDana() + " menjadi " + currMat.getSumberDana() + "/n";
                }
            }
             
            //26
            if (prevMat == null || (prevMat.getPlafonAwal() != currMat.getPlafonAwal())) {
                if (prevMat == null){
                    history=history+" Plafon Awal : "+currMat.getPlafonAwal()+"/n";
                }else{
                    history = history + " Plafon Awal diubah dari " + prevMat.getPlafonAwal() + " menjadi " + currMat.getPlafonAwal() + "/n";
                }
            }
            
            //27
            if (prevMat == null || (prevMat.getPlafon() != currMat.getPlafon())) {
                if (prevMat == null){
                    history=history+" Plafon : "+currMat.getPlafonAwal()+"/n";
                }else{
                    history = history + " Palfon diubah dari " + prevMat.getPlafon() + " menjadi " + currMat.getPlafon() + "/n";
                }
            }
            
            //28
            if (prevMat == null || (prevMat.getRealisasi() != currMat.getRealisasi())) {
                if (prevMat == null){
                    history=history+" Realisasi/Pencairan Bulan Berjalan : "+currMat.getRealisasi()+"/n";
                }else{
                    history = history + " Realisasi/Pencairan Bulan Berjalan diubah dari " + prevMat.getRealisasi() + " menjadi " + currMat.getRealisasi() + "/n";
                }
            }
            
            //29
            if (prevMat == null ||(prevMat.getDenda() != currMat.getDenda())) {
                if (prevMat == null){
                    history=history+" Denda : "+currMat.getDenda()+"/n";
                }else{
                    history = history + " Denda diubah dari " + prevMat.getDenda() + " menjadi " + currMat.getDenda() + "/n";
                }
            }
            
            //30
            if (prevMat == null || (prevMat.getBakiDebet() != currMat.getBakiDebet())) {
                if (prevMat == null){
                    history=history+" Baki Debet : "+currMat.getBakiDebet()+"/n";
                }else{
                    history = history + " Baki Debet diubah dari " + prevMat.getBakiDebet() + " menjadi " + currMat.getBakiDebet() + "/n";
                }
            }
            
            //31
//            if (prevMat == null || (prevMat.get != this.getNilaiUangAsal())) {
//                if (prevMat == null){
//                    history=history+" Nilai Dalam Mata Uang Asal : "+this.getNilaiUangAsal()+"/n";
//                }else{
//                    history = history + " Nilai Dalam Mata Uang Asal diubah dari " + prevMat.getNilaiUangAsal() + " menjadi " + this.getNilaiUangAsal() + "/n";
//                }
//            }
            
            //32
            if (prevMat == null || !prevMat.getKodeKolektibilitas().equals(currMat.getKodeKolektibilitas())) {
                if (prevMat == null){
                    history=history+" Kode Kolektibilitas : "+currMat.getKodeKolektibilitas()+"/n";
                }else{
                    history = history + " Kode Kolektibilitas diubah dari " + prevMat.getKodeKolektibilitas() + " menjadi " + currMat.getKodeKolektibilitas() + "/n";
                }
            }
            
            //33
            if(prevMat.getTglMacet()!=null){
                if (prevMat == null || !prevMat.getTglMacet().equals(currMat.getTglMacet())) {
                    if (prevMat == null){
                        history=history+" Tanggal Macet : "+currMat.getTglMacet()+"/n";
                    }else{
                        history = history + " Tanggal Macet diubah dari " + prevMat.getTglMacet() + " menjadi " + currMat.getTglMacet() + "/n";
                    }
                }
            }
            
            //34
            if(prevMat.getKodeSebabMacet()!=null){
            if (prevMat == null || !prevMat.getKodeSebabMacet().equals(currMat.getKodeSebabMacet())) {
                if (prevMat == null){
                    history=history+" Kode Sebab Macet : "+currMat.getTglMacet()+"/n";
                }else{
                    history = history + " Kode Sebab Macet diubah dari " + prevMat.getKodeSebabMacet() + " menjadi " + currMat.getKodeSebabMacet() + "/n";
                }
            }
            }
            
            //35
            if (prevMat == null || (prevMat.getTunggakanPokok() != currMat.getTunggakanPokok())) {
                if (prevMat == null){
                    history=history+" Tunggakan Pokok : "+currMat.getTunggakanPokok()+"/n";
                }else{
                    history = history + " Tunggakan Pokok diubah dari " + prevMat.getTunggakanPokok() + " menjadi " + currMat.getTunggakanPokok() + "/n";
                }
            }
            
            //36
            if (prevMat == null || (prevMat.getTunggakanBunga() != currMat.getTunggakanBunga())) {
                if (prevMat == null){
                    history=history+" Tunggakan Bunga : "+currMat.getTunggakanBunga()+"/n";
                }else{
                    history = history + " Tunggakan Bunga diubah dari " + prevMat.getTunggakanBunga() + " menjadi " + currMat.getTunggakanBunga() + "/n";
                }
            }
            
            //37
            if (prevMat == null || (prevMat.getJumlahHariTunggakan()!= currMat.getJumlahHariTunggakan())) {
                if (prevMat == null){
                    history=history+" Jumlah Hari Tunggakan : "+currMat.getJumlahHariTunggakan()+"/n";
                }else{
                    history = history + " Jumlah Hari Tunggakan diubah dari " + prevMat.getJumlahHariTunggakan() + " menjadi " + currMat.getJumlahHariTunggakan() + "/n";
                }
            }
            
            //38
            if (prevMat == null || (prevMat.getFrekuensiTunggakan() != currMat.getFrekuensiTunggakan())) {
                if (prevMat == null){
                    history=history+" Frkuensi Tunggakan : "+currMat.getFrekuensiTunggakan()+"/n";
                }else{
                    history = history + " Frkuensi Tunggakan diubah dari " + prevMat.getFrekuensiTunggakan() + " menjadi " + currMat.getFrekuensiTunggakan() + "/n";
                }
            }
            
            //39
            if (prevMat == null || (prevMat.getFrekuensiRestrukturisasi() != currMat.getFrekuensiRestrukturisasi())) {
                if (prevMat == null){
                    history=history+" Frkuensi Restrukturisasi : "+currMat.getFrekuensiRestrukturisasi()+"/n";
                }else{
                    history = history + " Frkuensi Restrukturisasi diubah dari " + prevMat.getFrekuensiRestrukturisasi() + " menjadi " + currMat.getFrekuensiRestrukturisasi() + "/n";
                }
            }
            
            //40
//            if (prevMat == null || !prevMat.gettgl().equals(currMat.getTglRestrukturisasiAwal())) {
//                if (prevMat == null){
//                    history=history+" Tanggal Restrukturisasi Awal : "+currMat.getTglRestrukturisasiAwal()+"/n";
//                }else{
//                    history = history + " Tanggal Restrukturisasi Awal diubah dari " + prevMat.getTglRestrukturisasiAwal() + " menjadi " + currMat.getTglRestrukturisasiAwal() + "/n";
//                }
//            }
            
            //41
//            if (prevMat == null || !prevMat.getTglRestrukturisasiAkhir().equals(currMat.getTglRestrukturisasiAkhir())) {
//                if (prevMat == null){
//                    history=history+" Tanggal Restrukturisasi Akhir : "+currMat.getTglRestrukturisasiAkhir()+"/n";
//                }else{
//                    history = history + " Tanggal Restrukturisasi Akhir diubah dari " + prevMat.getTglRestrukturisasiAkhir() + " menjadi " + currMat.getTglRestrukturisasiAkhir() + "/n";
//                }
//            }
            
            //42
//            if (prevMat == null || !prevMat.get().equals(currMat.getKodeCara())) {
//                if (prevMat == null){
//                    history=history+" Kode Cara Restrukturisasi : "+currMat.getKodeCara()+"/n";
//                }else{
//                    history = history + " Kode Cara Restrukturisasi diubah dari " + prevMat.getKodeCara() + " menjadi " + currMat.getKodeCara() + "/n";
//                }
//            }
            
            //43
            if (prevMat == null || !prevMat.getKodeKondisi().equals(currMat.getKodeKondisi())) {
                if (prevMat == null){
                    history=history+" Kode Kondisi : "+currMat.getKodeKondisi()+"/n";
                }else{
                    history = history + " Kode Kondisi diubah dari " + prevMat.getKodeKondisi() + " menjadi " + currMat.getKodeKondisi() + "/n";
                }
            }
            
            //44
            if (prevMat == null || !prevMat.getTglKondisi().equals(currMat.getTglKondisi())) {
                if (prevMat == null){
                    history=history+" Tanggal Kondisi : "+currMat.getTglKondisi()+"/n";
                }else{
                    history = history + " Tanggal Kondisi diubah dari " + prevMat.getTglKondisi() + " menjadi " + currMat.getTglKondisi() + "/n";
                }
            }
            
            //45
//            if (prevMat == null || !prevMat.getke().equals(currMat.getKeterangan())) {
//                if (prevMat == null){
//                    history=history+" Keterangan : "+currMat.getKeterangan()+"/n";
//                }else{
//                    history = history + " Keterangan diubah dari " + prevMat.getKeterangan() + " menjadi " + currMat.getKeterangan() + "/n";
//                }
//            }
            
            //46
            if (prevMat == null || !prevMat.getKodeKantorCabang().equals(currMat.getKodeKantorCabang())) {
                if (prevMat == null){
                    history=history+" Kantor Cabang : "+currMat.getKodeKantorCabang()+"/n";
                }else{
                    history = history + " Kantor Cabang diubah dari " + prevMat.getKodeKantorCabang() + " menjadi " + currMat.getKodeKantorCabang() + "/n";
                }
            }
            
            //47
//            if (prevMat == null || !prevMat.getOperasiData().equals(currMat.getOperasiData())) {
//                if (prevMat == null){
//                    history=history+" Operasi Data : "+currMat.getOperasiData()+"/n";
//                }else{
//                    history = history + " Operasi Data diubah dari " + prevMat.getOperasiData() + " menjadi " + currMat.getOperasiData() + "/n";
//                }
//            }
            
            //48
//            if (prevMat == null || !prevMat.getOpenDate().equals(currMat.getOpenDate())) {
//                if (prevMat == null){
//                    history=history+" Open Date : "+currMat.getOpenDate()+"/n";
//                }else{
//                    history = history + " Open Date diubah dari " + prevMat.getOpenDate() + " menjadi " + currMat.getOpenDate() + "/n";
//                }
//            }
            
            //49
//            if (prevMat.getStatusData() != currMat.getStatusData()) {
//                if (prevMat == null){
//                    history=history+" Status Data : "+currMat.getStatusData()+"/n";
//                }else{
//                    history = history + " Status Data diubah dari " + prevMat.getStatusData() + " menjadi " + currMat.getStatusData() + "/n";
//                }
//            }

        }catch (Exception e) {
            System.out.println(""+e.toString()+"");
            history="Eror";
        }
        //lanjutkan
        
        return history;
    }

}
